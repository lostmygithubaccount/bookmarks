#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."

echo "Formatting Rust code..."
cargo fmt --manifest-path crates/bookmarks-cli/Cargo.toml

echo "Rust formatting complete!"
